# Golden STT Clips

Generated locally on macOS via:

```bash
python3 backend/tests/generate_golden_clips.py
```

Clip set:
- `calibration_phrase.wav`
- `schedule_phrase.wav`
- `dashboard_phrase.wav`

All clips are mono 16 kHz PCM WAV so they are stable inputs for the regression suite.
